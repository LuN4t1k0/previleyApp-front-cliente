// // StatusBadge.js
// import React from "react";
// import { statusDefinitions } from "../../config/statusDefinitions";


// const StatusBadge = ({ status }) => {
  

//   const { text, bgColor, borderColor } = statusDefinitions[status] || {};

//   if (!text || !bgColor || !borderColor) {
//     return null; // Si el estado no es válido, no renderiza nada.
//   }

//   return (
//     <span
//       className={`inline-flex items-center gap-2.5 rounded-tremor-small ${bgColor} px-2.5 py-1 text-tremor-label font-light text-tremor-content-inverted`}
//     >
//       <span className="inline-flex items-center gap-1.5">{text}</span>
//     </span>
//   );
// };

// export default StatusBadge;


